{
  "name": "shell-protocol",
  "version": "0.1.0",
  "private": true,
  "description": "$SHELL Protocol — Decentralized AI Red Team Mining Network",
  "scripts": {
    "dev:web": "pnpm --filter @shell/web dev",
    "build:web": "pnpm --filter @shell/web build",
    "dev:miner": "pnpm --filter @shell/miner-cli dev"
  },
  "packageManager": "pnpm@10.6.0"
}
